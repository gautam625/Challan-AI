services:
  - type: web
    name: challan-ai
    env: python
    buildCommand: pip install python-bidi==0.4.2 && pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
